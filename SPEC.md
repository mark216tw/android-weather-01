# 簡單天氣 Android App 規格書

## 1. 專案概述

- App 名稱：簡單天氣
- 平台：Android
- 開發技術：Kotlin + Jetpack Compose
- 天氣資料來源：[Open-Meteo](https://open-meteo.com/)
- 顯示方向：僅支援直式螢幕
- 首版語言：繁體中文
- 首版單位：攝氏、公里／小時、毫米

「簡單天氣」以簡單、易用及容易單手操作為核心。App 優先使用裝置 GPS 取得目前位置，並提供城市搜尋作為定位失敗或拒絕權限時的替代方式。主畫面顯示現在及未來六天的天氣資訊，搭配依天氣及晝夜變化的靜態天空，營造沉浸感。

## 2. 首版功能範圍

### 2.1 核心功能

- 取得使用者目前 GPS 座標。
- 顯示目前位置或所選城市名稱。
- 顯示當下天氣資訊。
- 顯示明天天氣資訊。
- 顯示後天天氣資訊。
- 顯示今天至未來第六天天氣資訊。
- App 開啟時自動更新天氣。
- 從背景返回前景時，資料年齡達 15 分鐘才自動更新。
- App 持續在前景時，每 15 分鐘檢查一次；資料未滿 15 分鐘不重複請求。
- 支援下拉手動更新。
- 顯示最後更新時間。
- 定位失敗或權限遭拒時，可搜尋城市。
- 支援收藏多個城市並快速切換。
- 依天氣狀態及晝夜顯示不同動態天空。
- 網路失敗時優先顯示最近一次成功取得的快取資料。

### 2.2 天氣資訊

目前天氣顯示：

- 目前溫度
- 天氣描述與圖示
- 體感溫度
- 相對濕度
- 當下降雨機率
- 當下降雨量
- 風速
- 風向
- 風級及風級名稱，格式為「5級 · 清風 · 東北風」
- 日出時間
- 日落時間
- 當地時間與時區
- 今日預估日照時數
- 今日月相
- US AQI 與 PM2.5
- 海平面氣壓
- 能見度及分級描述
- 雲量及天空覆蓋描述
- 目前紫外線指數（UV Index）
- AQI 與 PM2.5 分級描述及標準顏色
- 海拔高度

明天、後天與大後天天氣顯示：

- 天氣描述與圖示
- 最高溫
- 最低溫
- 最高體感溫度
- 最低體感溫度
- 最大降雨機率
- 總降雨量
- 最大風速
- 日出時間
- 日落時間

### 2.3 首版不包含

- App 進入背景後的背景定時更新
- 天氣提醒通知
- 常駐定位追蹤
- 背景位置權限
- 英文介面
- 華氏或英制單位切換

## 3. 使用者流程

### 3.1 首次啟動

1. 顯示簡短定位用途說明。
2. 請求前景位置權限，優先接受約略位置。
3. 權限允許後，先嘗試取得最近位置，再請求目前位置。
4. 使用座標向 Open-Meteo 取得今天至未來第六天共七日天氣。
5. 顯示主畫面。

### 3.2 定位失敗

定位權限遭拒、GPS 關閉、定位逾時或無法取得座標時：

- 顯示清楚原因。
- 提供「重新定位」操作。
- 提供「搜尋城市」操作。
- 不強迫使用者開啟定位才能使用 App。

若使用者永久拒絕權限，可提供前往系統設定的入口，但城市搜尋仍可正常使用。

### 3.3 城市搜尋與收藏

1. 使用者開啟城市選擇 Bottom Sheet。
2. 輸入至少 2 個字元後開始搜尋。
3. 搜尋輸入使用約 300 毫秒 debounce，避免過度呼叫 API。
4. 顯示城市、行政區及國家名稱，避免同名城市混淆。
5. 點選結果後立即載入該城市天氣。
6. 使用者可加入或移除收藏。
7. 收藏城市可快速切換，並記住最後選擇。

收藏規則：

- 只有城市搜尋結果可加入收藏；「目前位置」本身不可收藏。若使用者希望保留所在地，需搜尋並收藏對應城市。
- 收藏以 Open-Meteo 地點 ID 作為唯一識別，同一地點不得重複收藏。
- 若搜尋結果缺少地點 ID，則以正規化後的城市名稱、國家代碼及四捨五入至小數點後 4 位的經緯度組合作為替代識別。正規化需去除首尾空白、將連續空白合併為一個、以 `Locale.ROOT` 轉為小寫；替代識別格式為 `fallback:{countryCode}:{name}:{latitude}:{longitude}`。
- 收藏依加入時間排序，較早加入者在前；首版不提供手動排序。
- 首版收藏數量上限為 20 個。達到上限時需提示使用者先移除既有收藏，不可靜默取代。
- 移除收藏只移除收藏狀態，不立即刪除該城市的天氣快取。
- 若移除目前正在查看的收藏城市，畫面仍保留該城市及天氣內容；使用者切換地點或重新啟動 App 後，不再將其列入收藏。
- App 記住最後選擇的來源模式及地點。最後選擇城市時，下次開啟仍顯示該城市；最後選擇目前位置時，下次開啟先顯示目前位置快取，再重新嘗試定位。

## 4. 畫面規格

### 4.1 主畫面

主畫面採單頁垂直捲動配置：

1. 頂部區域
   - 城市名稱
   - GPS／目前位置狀態
   - 當地時間（時：分：秒）及 IANA 時區與 GMT offset
   - 城市搜尋及切換入口
2. 目前天氣主視覺
   - 放大的手繪天氣圖案、目前溫度及天氣描述
   - 濕度及其下方的體感溫度
3. 天氣資訊卡
   - 單一半透明緊湊資訊區塊，採雙欄資訊列
   - 降雨機率、降雨、風速與風向、海拔高度
   - 日出、日落、日照時數、月相
   - 能見度與雲量置於風速與氣壓下一列
   - US AQI、PM2.5、氣壓、紫外線指數
   - 不重複顯示頁首已有的觀測地點及資料時區
4. 七日預報區域
   - 今天
   - 明天
   - 後天
   - 大後天
   - 後續三天以「星期X」顯示
5. 更新資訊
   - 最後更新時間
   - 資料來源 Open-Meteo

### 4.2 操作原則

- 主要操作元件點擊範圍至少 `48 dp`。
- 支援單手操作。
- 支援下拉更新。
- 避免過多層級與不必要的頁面跳轉。
- 文字與背景需符合可讀性及對比要求。
- 支援系統大字體，不使用容易截斷內容的固定高度。
- 處理瀏海、狀態列、導覽列及各種直式螢幕尺寸。
- 圖示及互動元件提供無障礙內容描述。

### 4.3 畫面狀態

介面需明確支援：

- `Loading`：首次載入。
- `Content`：成功顯示天氣。
- `Refreshing`：保留原內容並顯示更新狀態。
- `LocationPermissionRequired`：需要定位權限。
- `LocationUnavailable`：無法取得定位。
- `NetworkError`：無網路或 API 失敗。
- `EmptySearchResult`：找不到城市。

若已有快取資料，更新失敗時保留舊內容，以非阻斷式訊息提示失敗及資料更新時間，不使用全頁錯誤覆蓋內容。

## 5. 視覺設計

### 5.1 設計方向

- 風格：活潑、友善、乾淨、沉浸式。
- 主視覺：依實際天氣及晝夜變化的全螢幕靜態漸層天空。
- 日出後使用標準淺色主題，日落後使用標準深色主題；狀態列及導覽列需同步切換背景與圖示明暗。
- 天氣圖示使用暖色、圓潤且帶不規則筆觸的手繪風格，不使用依賴系統字型的 Emoji；月亮在深色背景需保持足夠對比。
- 資訊卡：半透明材質與柔和陰影。
- 目前天氣詳細資訊以單一半透明區塊中的緊湊雙欄資訊列呈現，標籤搭配一致的向量圖示及重點色，數值使用高對比粗體。
- 十項資料的數值使用較大的字級；風速與風向分成上下兩行，月相數值左側顯示月相圖例。
- AQI 描述顯示於數值下方，依 AQI 分級使用綠、黃、橘、紅、紫及褐紅色。
- PM2.5 顯示數值及分級描述，依 0.0–15.4、15.5–35.4、35.5–54.4、54.5–150.4 μg/m³ 區間顯示顏色與描述。
- 資訊卡透明度約 74%，仍需維持足夠文字對比。
- 天氣資訊維持高對比，背景不使用裝飾性動畫。

### 5.2 天氣場景

- 晴天：較鮮艷的藍色至暖黃色靜態漸層。
- 多雲：較鮮明的灰藍色靜態漸層。
- 雨天：較鮮艷的藍色靜態漸層。
- 雷雨：較鮮明的深紫藍色靜態漸層。
- 夜晚：依實際天氣使用鮮明靛藍、深藍或深紫藍靜態漸層。

優先以 Jetpack Compose 漸層及向量圖形實作，不加入大型影片、照片或背景動畫素材。

## 6. Open-Meteo API

### 6.1 天氣預報

端點：

```text
https://api.open-meteo.com/v1/forecast
```

查詢參數：

```text
latitude={latitude}
longitude={longitude}
timezone=auto
forecast_days=7
current=temperature_2m,apparent_temperature,relative_humidity_2m,precipitation_probability,precipitation,weather_code,wind_speed_10m,wind_direction_10m,pressure_msl,visibility,cloud_cover,uv_index,is_day
daily=weather_code,temperature_2m_max,temperature_2m_min,apparent_temperature_max,apparent_temperature_min,precipitation_probability_max,precipitation_sum,sunrise,sunset,sunshine_duration,moon_phase,wind_speed_10m_max,uv_index_max
```

規則：

- 使用 `timezone=auto` 取得該地點當地時間。
- 使用 `forecast_days=7` 取得今天至未來第六天資料；主畫面預報區顯示今天、明天、後天、大後天及後續三天。
- 溫度使用攝氏。
- 風速使用公里／小時。
- 降雨量使用毫米。
- WMO Weather Code 需轉換為繁體中文描述、圖示及背景場景。
- 使用 `is_day` 判斷目前為白天或夜晚。
- 晴朗（WMO code 0）使用單獨太陽圖示；大致晴朗及局部多雲（WMO code 1、2）使用太陽躲在雲後的圖示。
- 日期、時間及「今天／明天／後天／大後天／星期X」一律以 API 回傳的地點時區為準，不使用裝置目前時區判定。
- 保存 API 回傳的 IANA 時區名稱及 UTC offset；領域層以 IANA 時區轉換及格式化時間，UTC offset 僅供資料驗證及降級使用。
- `current.time`、`daily.time`、`sunrise` 及 `sunset` 皆視為該地點的本地時間。解析時需套用 API 回傳的時區，並正確處理夏令時間。
- daily 各欄位陣列依日期索引組合；若陣列長度不一致，僅建立所有必要欄位索引皆存在的日期，選填欄位缺少時保留為空值，不得造成 App 崩潰。
- 主題日夜狀態以地點時區的目前時間及當日日出、日落判定；資料缺少時才降級使用 `is_day`。
- 天氣圖示需區分日夜，晴朗或大致晴朗在日落後不得顯示太陽圖示。

### 6.2 城市搜尋

端點：

```text
https://geocoding-api.open-meteo.com/v1/search
```

查詢參數：

```text
name={keyword}
count=10
language=zh
format=json
```

搜尋結果至少保留：

- Open-Meteo 地點 ID
- 城市名稱
- 緯度
- 經度
- 時區
- 國家名稱及國家代碼
- 第一級行政區名稱

### 6.3 空氣品質

端點：

```text
https://air-quality-api.open-meteo.com/v1/air-quality
```

查詢參數：

```text
latitude={latitude}
longitude={longitude}
timezone=auto
current=us_aqi,pm2_5
```

- 空氣品質與天氣預報並行取得；空氣品質請求失敗不得阻擋天氣內容。
- 顯示 US AQI 分級及 PM2.5 濃度。
- AQI 分級顏色：0–50 綠色、51–100 黃色、101–150 橘色、151–200 紅色、201–300 紫色、301–500 褐紅色。
- PM2.5 分級顏色：0.0–15.4 綠色、15.5–35.4 黃色、35.5–54.4 橘色、54.5–150.4 紅色。
- 氣壓使用 `pressure_msl`，單位為 `hPa`。
- 氣壓分級：低於 980 hPa 為強烈低壓、980–1010.9 為低氣壓、1011–1019.9 為接近平均、1020–1039.9 為高氣壓、1040 以上為強高氣壓。此分級僅供閱讀，不得僅憑氣壓宣稱颱風。
- 能見度使用 `visibility`，由公尺轉換成公里；分為良好（10 km 以上）、普通（5–低於 10 km）、不佳（1–低於 5 km）及很差（低於 1 km）。
- 雲量使用 `cloud_cover`；分為晴朗（0–20%）、大致晴朗（21–40%）、局部多雲（41–60%）、多雲（61–80%）及陰天（81–100%）。
- 目前 UV 使用 `uv_index`，每日預報使用 `uv_index_max`。
- UV 分級為低量級（0–2）、中量級（3–5）、高量級（6–7）、過量級（8–10）、危險級（11+）。
- 需標示空氣品質資料來源為 CAMS / Open-Meteo。

### 6.4 GPS 地名來源

- GPS 取得座標後，使用繁體中文 Locale 的 Android `Geocoder` 進行反向地理編碼，不另行串接需要 API Key 的第三方地理編碼服務。
- Android 13（API 33）以上使用非同步 `Geocoder` API；Android 12L（API 32）以下以 coroutine 包裝同步呼叫，且不得在主執行緒執行。
- 地名優先順序為 `locality`、`subAdminArea`、`adminArea`、`countryName`，取第一個非空值作為主要顯示名稱；可取得行政區及國家時一併保存。
- 反向地理編碼逾時為 5 秒。服務不可用、逾時或查無結果時，不阻擋天氣查詢，地名顯示為「目前位置」。
- 反向地理編碼僅在取得新的可用座標時執行，不因畫面重組重複請求。

### 6.5 資料來源標示

主畫面底部或關於頁需顯示：

```text
天氣資料：Open-Meteo.com
```

## 7. Android 技術架構

### 7.1 建議技術

- 最低支援版本：Android 8.0（API 26）。
- 編譯版本：Android 16（API 36）。
- 目標版本：Android 16（API 36）。若 Google Play 在發布時要求更高版本，應升級至當時要求的版本並完成回歸測試。
- Kotlin
- Jetpack Compose
- Material 3
- 單 Activity 架構
- Navigation Compose
- ViewModel
- Kotlin Coroutines
- StateFlow
- Retrofit
- Kotlin Serialization
- Hilt
- Room
- DataStore
- Google Play Services Location
- `FusedLocationProviderClient`

### 7.2 分層

```text
UI / Compose
    ↓
ViewModel / UI State
    ↓
Use Case（僅在邏輯需要時建立）
    ↓
Repository
    ↓
Remote API / Location / Room / DataStore
```

職責：

- UI：顯示狀態並傳遞使用者事件。
- ViewModel：管理畫面狀態、更新流程與錯誤狀態。
- Repository：協調網路、定位及快取資料。
- Remote API：Open-Meteo 天氣與城市搜尋。
- Room：收藏城市與最近天氣快取。
- DataStore：目前選擇城市、首次定位說明等輕量設定。

避免為簡單邏輯建立過多抽象層；僅在具有明確重用或測試價值時建立 Use Case。

### 7.3 建議資料模型

```text
Location
- id
- name
- adminArea
- country
- latitude
- longitude
- timezone
- isCurrentLocation
- isFavorite

CurrentWeather
- observedAt
- temperature
- apparentTemperature
- relativeHumidity
- precipitationProbability
- precipitation
- weatherCode
- windSpeed
- windDirection
- pressureMsl
- visibilityMeters
- cloudCover
- uvIndex
- isDay

DailyForecast
- date
- weatherCode
- maxTemperature
- minTemperature
- maxApparentTemperature
- minApparentTemperature
- precipitationProbability
- precipitationSum
- sunrise
- sunset
- maxWindSpeed
```

## 8. 定位與權限

- Manifest 宣告前景位置權限。
- 不請求背景位置權限。
- 優先支援約略位置，App 不應要求精確位置才能使用。
- 進入「目前位置」模式時先讀取最近位置。最近位置的資料年齡不超過 30 分鐘且水平精度不大於 10 公里時，可直接作為本次查詢座標。
- 最近位置不符合條件時，使用 `FusedLocationProviderClient.getCurrentLocation` 請求目前位置，接受約略位置，逾時為 10 秒。
- 若目前位置請求失敗，但存在 24 小時內的最近位置，需先告知使用者定位資料較舊，再允許使用該座標查詢；超過 24 小時則視為定位不可用。
- 使用可用座標後即結束定位流程，不為提高精度而持續等待或重複請求。
- App 啟動時，只有最後選擇為「目前位置」模式才重新執行定位；若最後選擇固定城市，不主動要求定位權限或取得位置。
- 定位逾時、取消、提供者關閉及權限拒絕需映射為不同內部原因，但介面文字保持簡短並提供重新定位及搜尋城市操作。
- 不持續監聽位置，降低電量消耗。
- 權限拒絕後仍可透過城市搜尋完整使用 App。
- 清楚說明定位僅用於查詢所在地天氣。

## 9. 資料快取與更新

- App 啟動時更新目前選擇地點。
- 使用者下拉時強制嘗試更新。
- 從背景返回前景時，若目前資料已達 15 分鐘，立即自動更新。
- 持續在前景時每 15 分鐘檢查資料年齡，達 15 分鐘才更新。
- 進入背景後停止前景週期檢查。
- 城市切換後立即載入快取並更新網路資料。
- 成功回應後更新 Room 快取及最後更新時間。
- 網路失敗時顯示同一地點最近一次快取。
- 沒有快取且網路失敗時顯示可重試錯誤畫面。
- 不使用 WorkManager 定期背景更新；自動更新只在 App 前景期間執行。

快取識別與生命週期：

- 城市天氣快取鍵使用 `city:{Open-Meteo 地點 ID}`；地點 ID 缺少時，使用 `city:{替代識別}`，替代識別依城市收藏規則產生。
- 目前位置使用固定快取鍵 `current_location`，每次成功取得新座標及天氣後覆寫。快取內容需同時保存實際查詢經緯度、顯示地名及時區，避免座標與天氣資料不一致。
- 天氣快取必須保存資料取得時間 `fetchedAt`。時間以 UTC Instant 儲存，顯示時再轉換為查詢地點的時區。
- 快取超過 2 小時視為過舊，但仍可在網路失敗時顯示，並明確標示最後更新時間及「資料可能已過期」。
- 切換地點時只可讀取相同快取鍵的資料，不得以其他城市或先前 GPS 地點的資料暫代。
- 非收藏城市快取可保留 30 天；超過 30 天可在 App 啟動後進行非阻斷式清理。收藏城市快取在收藏存在期間不因時間自動刪除。`current_location` 僅保留最後一次成功資料並於更新時覆寫。
- 收藏狀態與天氣快取分開保存；移除收藏不立即刪除快取，該快取後續依非收藏城市的 30 天規則清理。

## 10. App 圖示規格

### 10.1 尺寸

- 最終 Adaptive Icon 圖層尺寸：`108 × 108 dp`
- 外框裁切參考區：`72 × 72 dp`
- 核心安全區：`66 × 66 dp`
- 太陽核心圖案占安全區約 90%，約 `59.4 × 59.4 dp`

### 10.2 視覺

- 核心圖案：位於中央、縮小後的黃橘色手繪太陽。
- 風格：活潑、友善、黃橘色扁平向量圖示。
- 太陽中央保留多層不規則黃橘同心圓。
- 外圍使用約 12 道連續、圓弧過渡的尖角日芒，參考附圖但不完全複製。
- 外圍日芒使用亮黃色，形成完整連續的太陽輪廓。
- 不使用黑色或深色外框。
- 背景使用暖白色，與黃橘色太陽維持清楚對比。
- 完整圖案需位於 Adaptive Icon 核心安全區，並在圓形、圓角方形及其他 Launcher Mask 下保持完整。

### 10.3 Android 資源

- Adaptive Icon 前景層。
- Adaptive Icon 背景層。
- Android 13 monochrome icon。
- 舊版 Android legacy launcher icon。

## 11. 直式限制

主要 Activity 在 Android Manifest 固定為直式：

```xml
android:screenOrientation="portrait"
```

固定直式不代表固定版面尺寸。所有畫面仍須支援不同寬高比例、小螢幕、平板直式、大字體、瀏海及系統列 Insets。

## 12. 錯誤處理

- API 逾時：顯示簡短提示並保留快取。
- API 回傳錯誤：允許重試，不使 App 崩潰。
- API 欄位缺少：以可空模型安全處理，隱藏無法顯示的欄位。
- 城市查無結果：顯示「找不到符合的城市」。
- 定位關閉：顯示重新定位及搜尋城市選項。
- 永久拒絕定位：提供系統設定入口及城市搜尋。
- 快取過舊：仍可顯示，但明確標示最後更新時間。

## 13. 測試計畫

### 13.1 單元測試

- WMO Weather Code 轉換。
- API DTO 至領域模型映射。
- 溫度、風速、降雨及日期格式化。
- 海平面氣壓、能見度、雲量、UV、AQI 及 PM2.5 分級邊界。
- 今天至未來第六天的日期對應、七日篩選及「星期X」標題。
- API 地點時區、夏令時間及裝置時區不同時的日期與時間轉換。
- 城市 ID 與缺少 ID 時的替代識別、收藏去重及 20 個上限。
- 最近位置的新鮮度、精度、24 小時降級及定位逾時判斷。
- 城市與目前位置快取鍵、過舊標示及快取清理規則。
- Repository 成功、失敗、逾時及快取回退。
- ViewModel 載入、更新、定位拒絕及城市切換狀態。

### 13.2 UI 測試

- 顯示今天、明天、後天、大後天及後續三天天氣。
- 下拉更新。
- 城市搜尋及空結果。
- 新增、切換及移除收藏城市。
- 定位權限允許、拒絕及永久拒絕。
- GPS 地名反查成功、逾時與查無結果時的降級顯示。
- 無網路時顯示快取。
- 沒有快取時顯示重試畫面。

### 13.3 視覺與裝置驗證

- 晴天、多雲、雨天、雷雨及夜晚場景。
- 小螢幕及不同直式比例。
- 系統大字體。
- 深色系統列及導覽列。
- 減少動畫設定。
- Adaptive Icon 在不同 Launcher Mask 下的裁切結果。

## 14. 驗收條件

- App 僅以直式顯示。
- 使用者允許定位後，可取得所在地今天至未來第六天共七日天氣。
- 使用者拒絕定位後，仍可搜尋並查看城市天氣。
- 主畫面清楚顯示今天、明天、後天、大後天及後續三天資訊。
- 體感溫度、降雨、風速、日出及日落可正常顯示。
- 氣壓、能見度、雲量、目前 UV 及每日最大 UV 可正常顯示；資料缺少時顯示 `--`。
- 可收藏、切換及移除多個城市。
- 同一城市不可重複收藏，收藏達 20 個時會顯示上限提示，移除目前查看的收藏不會立即清除畫面。
- App 啟動及下拉操作可更新資料。
- 更新失敗且有快取時，舊資料仍可閱讀。
- 快取超過 2 小時會標示可能過期，且不同地點的快取不會互相替代。
- GPS 地名反查失敗時仍可使用座標取得天氣，並顯示「目前位置」。
- 今天、明天、後天、大後天、星期標題、日出與日落皆依查詢地點時區顯示，不受裝置時區影響。
- 天氣及晝夜變化會切換對應靜態背景。
- 目前天氣資訊可在一般手機螢幕的首個畫面內閱讀，不需為查看目前資料向下滑動。
- App 圖示符合 Adaptive Icon、安全區及黃橘手繪太陽主題規格。
- 定位、網路及資料錯誤不會造成 App 崩潰。

## 15. 建議實作順序

1. 建立 Android Compose 專案、Material 3 Theme、版本管理及直式限制。
2. 建立 Open-Meteo API、DTO、領域模型及 Repository。
3. 建立 WMO Weather Code 對應及資料格式化。
4. 完成 GPS 權限與定位流程。
5. 完成主畫面及載入、內容、更新與錯誤狀態。
6. 加入城市搜尋。
7. 加入 Room 收藏城市、快取及 DataStore 設定。
8. 加入天氣與晝夜動態插畫。
9. 製作 Adaptive Icon、monochrome icon 及 legacy icon。
10. 補齊單元測試、Compose UI 測試及無障礙內容。
